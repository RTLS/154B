% airfoiltools.com
% Equation for airfoil
% Define x and z and area for each spar
% Define number of stringers in each zone
% Define location of each stringer

for i = 1:10
   for j = 1:4
        wings(i).sparCaps(j).posX=2;
        wings(i).sparCaps(j).posY=1;
   end
end